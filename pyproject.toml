[project]
name = "openproject-mcp-server"
version = "0.0.1"
description = "OpenProject MCP server (stdio only)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["mcp>=1.2.0"]

[project.scripts]
openproject-mcp = "openproject_mcp.main:run"
